services:
  api:
    build:
      context: .
      target: api
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
  web:
    build:
      context: .
      target: web
    ports:
      - "8080:80"
    depends_on:
      - api
